<%= link_to gravatar_for(current_user, size: Settings.image_size_50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("global.menu.profile"), current_user %></span>
<span><%= pluralize current_user.microposts.count, t("pages.microposts.singular_name") %></span>
